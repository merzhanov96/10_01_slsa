name: SLSA Go Builder

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  actions: read
  id-token: write

jobs:
  build:
    uses: slsa-framework/slsa-github-generator/.github/workflows/builder_go_slsa3.yml@v2.1.0
    with:
      go-version: 1.19
      config-file: .slsa-goreleaser.yml
      envs: "GOOS=linux,GOARCH=amd64,CGO_ENABLED=0"
